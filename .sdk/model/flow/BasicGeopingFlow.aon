# BasicGeopingFlow

main: kit: flow: BasicGeopingFlow:
{
  entity: "geoping",
  kind: "basic",
  name: "BasicGeopingFlow",
  step: [
    {
      input: {
        ref: "geoping_ref01",
        srcdatavar: "geoping_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "geoping01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-geoping_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicGeopingFlow"
}